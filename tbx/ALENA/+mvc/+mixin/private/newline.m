function nl = newline

%For backward compat with 2015b, need to define what we mean by newline.
% If / when backward compat no longer required, simply remove this function from codebase.
nl = char(10);

end
